import { ClientsInfoComponentModel } from './clients-info.component.model';

describe('ClientsInfoComponentModel', () => {
  it('should create an instance', () => {
    expect(new ClientsInfoComponentModel()).toBeTruthy();
  });
});

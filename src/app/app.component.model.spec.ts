import { AppComponentModel } from './app.component.model';

describe('AppComponentModel', () => {
  it('should create an instance', () => {
    expect(new AppComponentModel()).toBeTruthy();
  });
});
